export const SCROLL_CONFIG = {
  speed: 0.8, // Lower number = faster scroll
  smoothness: 0.1, // Lower = more immediate response
  normalizeWheel: true,
  mobile: {
    speed: 1.0,
    enabled: false // Disable smooth scroll on mobile
  }
}